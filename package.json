{
  "name": "query-params-viewer",
  "private": true,
  "version": "1.0.0",
  "description": "Build, regression tests and MCP server for query-params-viewer. The generated static pages still have no runtime dependencies; the dependencies below belong to the MCP server in mcp-server/, and jsdom is used only to exercise the pages in tests.",
  "bin": {
    "query-params-viewer-mcp": "./mcp-server/index.js"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node scripts/build.js && node scripts/test.js && node mcp-server/test.js",
    "mcp": "node mcp-server/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
